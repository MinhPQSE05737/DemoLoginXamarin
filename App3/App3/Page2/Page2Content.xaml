<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App3.Page2.Page2Content"
              xmlns:viewModel="clr-namespace:App3.ViewModels"
             x:Name="abc"
             >
    <ContentPage.BindingContext>
        <viewModel:LoginViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout
            BindingContext="{Binding Source={x:Reference abc}}">
            <Label
            Text="{Binding Email}">
            </Label>
            <Label
            Text="{Binding Password}">
            </Label>
            <Label 
            Text="Welccome To My Page">
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>